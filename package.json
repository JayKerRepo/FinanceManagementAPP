{
  "name": "expenseiq-nextjs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "clean": "node -e \"const fs = require('fs'); const path = require('path'); ['/.next', '/tsconfig.tsbuildinfo'].forEach(p => { try { const fullPath = path.join(process.cwd(), p); if (fs.existsSync(fullPath)) { fs.rmSync(fullPath, { recursive: true, force: true }); console.log('Deleted', p); } } catch(e) { console.error('Error deleting', p, e.message); } });\"",
    "clean:force": "node -e \"const fs = require('fs'); const path = require('path'); const rimraf = (dir) => { try { if (fs.existsSync(dir)) { fs.readdirSync(dir).forEach(f => { const curPath = path.join(dir, f); if (fs.lstatSync(curPath).isDirectory()) { rimraf(curPath); } else { fs.unlinkSync(curPath); } }); fs.rmdirSync(dir); } } catch(e) { } }; ['.next', 'tsconfig.tsbuildinfo', 'node_modules/.cache'].forEach(p => { const fullPath = path.join(process.cwd(), p); rimraf(fullPath); console.log('Cleaned', p); });\"",
    "prebuild": "npm run clean:force",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "lucide-react": "^0.344.0",
    "next": "^15.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.8.0",
    "react-spring": "^9.7.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.18",
    "eslint": "^8.57.0",
    "eslint-config-next": "^15.0.0",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.5.3"
  }
}
